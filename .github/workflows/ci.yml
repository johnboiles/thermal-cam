name: CI

on:
  push:
  pull_request:

jobs:
  macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - name: Install USB dependency
        run: brew install libusb
      - name: Build and test
        run: swift test
